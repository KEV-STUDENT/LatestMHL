<UserControl x:Class="MHLControls.DirectoryPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MHLControls" d:DataContext="{d:DesignInstance Type=local:DirectoryPicker}"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{Binding Path=CaptionWidth}"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="30" MaxHeight="30" MinHeight="30"/>
        </Grid.RowDefinitions>

        <Button Content=". . ." Margin="0,0,0,0" Grid.Column="2" Height="30" VerticalAlignment="Top" HorizontalAlignment="Center" Width="30" Click="Button_Click" />
        <local:MHLLabel Content="{Binding Path=Caption}" Margin="0,0,5,0"/>
        <local:MHLTextBox Grid.Column="1" Text="{Binding Path=Value}" Margin="0,0,0,0" IsReadOnly="True" />
    </Grid>
</UserControl>
